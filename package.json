{
  "name": "campus-connect-portal",
  "version": "1.0.0",
  "description": "Root workspace to run frontend and backend concurrently",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"cd backend && uv run uvicorn main:app --reload --host 0.0.0.0 --port 3000\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
